# Điểm cắm DUY NHẤT để trỏ frontend sang backend.
#
# - Để TRỐNG  -> app dùng mock localStorage tích hợp sẵn (demo được ngay, không cần setup gì).
# - MockAPI   -> dán base URL dạng: https://<project-id>.mockapi.io/api/v1
# - Spring Boot thật -> ví dụ: http://localhost:8080/api/v1
#
# Khi backend xong: chỉ cần đổi biến này + cập nhật auth trong src/lib/apiClient.ts (mock login -> JWT).
VITE_API_BASE_URL=
