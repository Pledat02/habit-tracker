spring:
    application:
        name: habit-tracker
   
    datasource:
        url: jdbc:postgresql://db.fqvexwqafzdunvyvdzhx.supabase.co:5432/postgres?sslmode=require
        username: postgres
        password: '${DB_PASSWORD}'
        driver-class-name: org.postgresql.Driver

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect